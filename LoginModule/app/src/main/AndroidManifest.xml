<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.loginmodule">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/accessooss"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/accessooss_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuFinal" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".CreateAccount" />
        <activity android:name=".OrdersFrame" />
        <activity android:name=".LockerFrame" />
        <activity android:name=".CalendarFrame" />
        <activity android:name=".MembersFrame" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>